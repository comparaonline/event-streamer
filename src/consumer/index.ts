// Legacy consumer (backward compatibility)
export { ConsumerRouter } from './legacy-consumer';

// Schema Registry consumer (new functionality)
export { SchemaRegistryConsumerRouter } from './schema-registry-consumer';
